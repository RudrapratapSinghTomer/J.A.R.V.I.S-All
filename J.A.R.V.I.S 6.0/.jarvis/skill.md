### Tool Limitation

You MUST ONLY use the tool names listed below. 

DO NOT hallucinate tools like 'openai', 'nim', or 'huggingface'.

If you want to use the 'openai' Python library, you must:
1. Write a .py script using 'write_file'.
2. Execute it using 'run_command' (e.g., "python my_script.py").