# Jarvis 8.0 Configuration

models:
  local:
    endpoint: "http://localhost:11434/api"
    slm: "qwen2.5:7b" # Switched from qwen2.5:7b — faster for Edge Guard routing
    vlm: "llama3.2-vision:11b"
  cloud:
    api_base: "https://integrate.api.nvidia.com/v1"
    # Note: NVIDIA NIM API key should be set via environment variable NVIDIA_API_KEY
    lrm: "meta/llama-3.3-70b-instruct" # or nvidia/nemotron-3-super-120b-a12b
    moe: "mistralai/mixtral-8x22b-instruct-v0.1"

routing:
  semantic_threshold: 0.7 # >0.7 routes to cloud LRM, <=0.7 routes to local SLM

security:
  # Jarvis 8.0 can write anywhere within the J.A.R.V.I.S All repo
  allowed_write_directory: "C:/Users/developer/Desktop/J.A.R.V.I.S All"

docker:
  sandbox_image: "python:3.10-slim"
